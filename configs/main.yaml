# Main configuration file
defaults:
  - model: resnet18
  - dataset: cifar10
  - optimizer: sgd
  - _self_

# Experiment settings
seed: 42
epochs: 100
batch_size: 128
num_workers: 4

# Output
output_dir: results/raw
log_interval: 10
