% 1.58-bit Convolutional Neural Networks: A Systematic Comparison Study
% Flexible template - can adapt to various conference styles
%
% Usage:
%   - For arXiv/preprint: use as-is with article class
%   - For conference: replace documentclass and add style file
%   - Common targets: NeurIPS, ICML, CVPR, ICLR, AAAI

\documentclass[10pt]{article}

% =============================================================================
% CONFERENCE ADAPTATION
% =============================================================================
% Uncomment ONE of these blocks when targeting a specific venue:
%
% --- NeurIPS ---
% \usepackage{neurips_2024}  % or neurips_2025
%
% --- ICML ---
% \usepackage{icml2024}
%
% --- CVPR/ICCV ---
% \usepackage[review]{cvpr}  % or [final] for camera-ready
%
% --- ICLR ---
% \usepackage{iclr2024_conference}
%
% =============================================================================

% Page geometry - moderate compression for preprint
\usepackage[margin=1in, top=0.9in, bottom=0.9in]{geometry}

% Core packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{cleveref}

% Algorithm packages (optional, uncomment if needed)
% \usepackage{algorithm}
% \usepackage{algorithmic}

% Figure/table placement
\usepackage{float}

% Subcaptions for multi-panel figures
\usepackage{subcaption}

% Handle missing files gracefully (for skeleton builds before analysis runs)
\newcommand{\inputifexists}[1]{%
  \IfFileExists{#1}{\input{#1}}{%
    \textit{[Placeholder: run analysis pipeline to generate this table]}%
  }%
}
\newcommand{\includefigureifexists}[2]{%
  \IfFileExists{#1}{\includegraphics[#2]{#1}}{%
    \fbox{\parbox{0.6\linewidth}{\centering\textit{[Placeholder figure]\\Run analysis pipeline to generate}}}%
  }%
}

% =============================================================================
% CUSTOM COMMANDS
% =============================================================================

% Model/method names
\newcommand{\bitnet}{BitNet}
\newcommand{\bitnetb}{BitNet b1.58}

% Math shortcuts
\newcommand{\real}{\mathbb{R}}
\newcommand{\expect}{\mathbb{E}}

% Highlight for TODOs (remove before submission)
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}
\newcommand{\fixme}[1]{\textcolor{orange}{[FIXME: #1]}}

% =============================================================================
% METADATA
% =============================================================================

\title{When Augmentation Fails:\\Knowledge Distillation for Ternary CNNs}

\author{
  Dario Cazzani\\
  Cisco Systems Inc.\\
  \texttt{dcazzani@cisco.com}
}

\date{}

% =============================================================================
% DOCUMENT
% =============================================================================

\begin{document}

\maketitle

% =============================================================================
% ABSTRACT
% =============================================================================

\begin{abstract}
Deploying neural networks on edge devices demands extreme compression, yet ternary quantization (1.58-bit weights) typically incurs a 3--4\% accuracy gap versus full-precision models. We investigate whether data augmentation---a standard technique for improving generalization---can close this gap. Surprisingly, it cannot: the gap remains approximately 3.5\% regardless of augmentation strategy. Through layer-wise ablation, we identify the first convolutional layer as responsible for 54--74\% of the accuracy loss across configurations. Combining a simple fix (keeping \texttt{conv1} in FP32, adding only 0.08\% parameters) with knowledge distillation, we recover 88\% of the accuracy gap on CIFAR-10 and \emph{exceed FP32 accuracy} on harder tasks: +1.0\% on CIFAR-100 (100 classes) and +1.3\% on Tiny ImageNet (200 classes). Our recipe enables practical ternary CNN deployment with near-full-precision---or better---accuracy while preserving $20\times$ compression.
\end{abstract}

% =============================================================================
% 1. INTRODUCTION
% =============================================================================

\section{Introduction}
\label{sec:intro}

Edge deployment of neural networks demands aggressive compression. Ternary quantization---restricting weights to $\{-1, 0, +1\}$---promises $20\times$ compression and $64\times$ theoretical speedup through integer-only arithmetic~\cite{ma2024bitnetb158}. For large language models, \bitnetb{} achieves performance matching FP16 transformers. But what happens when we apply this approach to convolutional neural networks?

We find a stubborn accuracy gap. Across ResNet architectures on CIFAR-10/100, ternary CNNs consistently underperform their FP32 counterparts by 3--4\%. This gap persists regardless of model capacity (ResNet-18 vs ResNet-50) and dataset complexity (10 vs 100 classes).

\textbf{The obvious solution fails.} A natural hypothesis is that ternary networks, with their constrained representational capacity, might benefit more from data augmentation. Stronger augmentation could act as regularization, helping the limited-capacity model generalize better. We test this systematically across four augmentation strategies: basic (random crop + flip), Cutout~\cite{devries2017cutout}, RandAugment~\cite{cubuk2020randaugment}, and their combination.

The result is surprising: \emph{augmentation does not close the gap}. Whether we use minimal or aggressive augmentation, the accuracy deficit remains approximately 3.5\%. This ``augmentation paradox'' suggests the gap stems from a fundamental capacity limitation, not insufficient regularization.

\textbf{Diagnosis: the first layer matters most.} To understand where the accuracy loss originates, we conduct layer-wise ablation: selectively keeping individual layers in FP32 while quantizing the rest. While keeping first and last layers at higher precision is established practice in binary/ternary networks~\cite{rastegari2016xnornet,zhou2016dorefa}, we provide the first \emph{precise quantification} for \bitnetb{}: keeping only the first convolutional layer (\texttt{conv1}) in FP32 recovers 54--74\% of the accuracy gap across all configurations---despite \texttt{conv1} comprising just 0.08\% of parameters. Other layers (residual blocks, final classifier) provide minimal benefit.

Why \texttt{conv1}? The first layer processes raw pixel values, where subtle intensity gradients carry critical information. Ternary weights, restricted to three values, cannot capture these fine-grained patterns. Deeper layers operate on increasingly abstract features that tolerate coarse quantization. This validates the long-standing convention with empirical rigor.

\textbf{A practical recipe.} Building on this insight, we combine the \texttt{conv1} fix with knowledge distillation from an FP32 teacher. The effects are largely additive: \texttt{conv1} alone recovers 58\%, KD alone recovers 36\%, and together they recover 88\% of the gap on CIFAR-10. Remarkably, on harder tasks, this recipe \emph{exceeds FP32 accuracy}: +1.0\% on CIFAR-100 (100 classes) and +1.3\% on Tiny ImageNet (200 classes, 64$\times$64 resolution). This demonstrates that KD's regularization can push ternary networks beyond full-precision performance, with the effect strengthening as task complexity increases.

\textbf{Contributions.}
\begin{enumerate}
    \item \textbf{The augmentation paradox}: We demonstrate that data augmentation fails to close the ternary-FP32 accuracy gap, ruling out regularization as a solution.
    \item \textbf{Quantified layer sensitivity}: While keeping early layers in FP32 is established practice, we provide the first precise quantification: \texttt{conv1} accounts for 54--74\% of accuracy loss with only 0.08\% of parameters.
    \item \textbf{A practical recipe}: We show that keeping \texttt{conv1} in FP32 combined with knowledge distillation recovers 88\% of the accuracy gap on CIFAR-10 and \emph{exceeds FP32 accuracy} on harder tasks (CIFAR-100, Tiny ImageNet)---validating a simple mixed-precision strategy without expensive architecture search.
    \item \textbf{Fully reproducible}: We release a complete code-to-paper pipeline where a single command regenerates all experiments, tables, figures, and this document from raw data. All 195+ experiments are deterministic and verifiable.
\end{enumerate}

% =============================================================================
% 2. RELATED WORK
% =============================================================================

\section{Related Work}
\label{sec:related}

\textbf{Binary and Ternary CNNs.}
BinaryConnect~\cite{courbariaux2015binaryconnect} pioneered binary weight training using the straight-through estimator (STE). XNOR-Net~\cite{rastegari2016xnornet} extended this to binary activations, achieving $58\times$ speedup but with significant accuracy loss. Ternary Weight Networks (TWN)~\cite{li2016ternary} demonstrated that adding zero improves over strict binary. Trained Ternary Quantization (TTQ)~\cite{zhu2017ttq} learned \emph{two asymmetric scaling factors per layer} ($W^p$ for positive, $W^n$ for negative weights), achieving near-FP32---and sometimes improved---accuracy on CIFAR-10 ResNets. Notably, TTQ keeps first and last layers in full precision, a practice we analyze quantitatively. Our work differs by using the simpler \bitnetb{} formulation (fixed $\{-1,0,+1\}$ without learned scales) and systematically studying what \emph{doesn't} work (augmentation) alongside what does (conv1 + KD).

\textbf{BitNet for Vision.}
BitNet~\cite{wang2023bitnet} introduced 1-bit quantization for transformers, and \bitnetb{}~\cite{ma2024bitnetb158} refined this to ternary weights, matching FP16 LLM performance with $20\times$ compression. For vision, Nielsen and Schneider-Kamp~\cite{nielsen2024bitnetreloaded} applied \bitnetb{} to small custom CNNs (100K--2.2M parameters) trained for only 10 epochs, achieving 71.47\% on CIFAR-10. Concurrently, BD-Net~\cite{kim2025bdnet} applied 1.58-bit quantization to MobileNet's depthwise convolutions. However, no prior work has systematically studied \bitnetb{} on standard architectures (ResNet) with full training schedules, nor explored the interaction between data augmentation and ternary quantization.

\textbf{Knowledge Distillation for Quantization.}
Hinton et al.~\cite{hinton2015distilling} introduced knowledge distillation via soft labels. For quantized models, QKD~\cite{kim2019qkd} showed distillation helps binary networks. Recent work suggests feature-level distillation (matching intermediate activations) can provide 5--20\% additional improvement over logit-only KD for extremely quantized networks~\cite{elthakeb2020dcq}. We use standard soft-label distillation ($T=4$, $\alpha=0.9$), which works out-of-the-box without task-specific tuning, leaving feature distillation as promising future work.

\textbf{Layer-wise Quantization Sensitivity.}
Keeping first and last layers at higher precision has been standard practice since XNOR-Net~\cite{rastegari2016xnornet} and DoReFa-Net~\cite{zhou2016dorefa}, motivated by error propagation concerns and the observation that these layers ``constitute a very small portion of overall computation.'' Mixed-precision approaches like HAQ~\cite{wang2019haq} and HAWQ~\cite{dong2019hawq} learn per-layer bit-widths via reinforcement learning or Hessian analysis. While keeping \texttt{conv1} in FP32 is established practice, we provide the first \emph{precise quantification} for ternary CNNs: \texttt{conv1} accounts for 54--74\% of accuracy degradation despite comprising only 0.08\% of parameters. This validates the convention with empirical rigor and motivates a simple mixed-precision strategy without expensive search.

% =============================================================================
% 3. METHOD
% =============================================================================

\section{Method}
\label{sec:method}

\subsection{BitNet Layer Implementation}

We implement \bitnetb{} for both linear and convolutional layers. The forward pass consists of three steps:

\textbf{1. Weight Quantization.} Weights are quantized to $\{-1, 0, +1\}$ using:
\begin{equation}
    \tilde{W} = \text{RoundClip}\left(\frac{W}{\gamma + \epsilon}, -1, 1\right), \quad \gamma = \frac{1}{n}\sum_{i}|W_i|
\end{equation}
where $\gamma$ is the mean absolute value and $\epsilon=10^{-5}$ ensures numerical stability.

\textbf{2. Activation Quantization.} Activations are quantized to 8-bit using absmax quantization:
\begin{equation}
    \tilde{x} = \text{Clip}\left(x \cdot \frac{Q_b}{\gamma_x}, -Q_b, Q_b\right), \quad Q_b = 2^{b-1}
\end{equation}
where $\gamma_x = \max(|x|)$ and $b=8$ bits.

\textbf{3. Dequantization.} After the matrix/convolution operation, outputs are scaled back:
\begin{equation}
    y = \frac{\gamma \cdot \gamma_x}{Q_b} \cdot (\tilde{W} \cdot \tilde{x})
\end{equation}

\textbf{Gradient Flow.} The straight-through estimator (STE) allows gradients to flow through quantization by using the identity function in the backward pass.

\subsection{Model Conversion}

We convert standard architectures from the \texttt{timm} library~\cite{rw2019timm} by replacing all \texttt{nn.Linear} and \texttt{nn.Conv2d} layers with their BitNet equivalents. Batch normalization and activation functions remain in FP32.

\subsection{Training Protocol}

All models are trained from scratch with:
\begin{itemize}
    \item Optimizer: SGD with momentum 0.9 and Nesterov acceleration
    \item Learning rate: 0.1 with cosine annealing
    \item Weight decay: $5 \times 10^{-4}$
    \item Epochs: 200
    \item Batch size: 128
\end{itemize}

% =============================================================================
% 4. WHAT DOESN'T WORK: THE AUGMENTATION PARADOX
% =============================================================================

\section{What Doesn't Work: The Augmentation Paradox}
\label{sec:augmentation}

A natural hypothesis is that ternary networks, with limited representational capacity, might benefit disproportionately from data augmentation. Stronger augmentation could regularize the constrained model, helping it generalize better and potentially closing the accuracy gap.

\subsection{Experimental Setup}

\textbf{Datasets.} We evaluate on CIFAR-10 (10 classes, 50K training images) and CIFAR-100 (100 classes, 50K training images).

\textbf{Models.} We use ResNet-18 and ResNet-50~\cite{he2016resnet}, representing different model capacities.

\textbf{Augmentation Strategies.} We test four configurations of increasing intensity:
\begin{itemize}
    \item \textbf{Basic}: RandomCrop(32, padding=4) + RandomHorizontalFlip
    \item \textbf{Cutout}: Basic + RandomErasing(p=0.5)~\cite{devries2017cutout}
    \item \textbf{RandAugment}: Basic + RandAugment(n=2, m=9)~\cite{cubuk2020randaugment}
    \item \textbf{Full}: RandAugment + RandomErasing (most aggressive)
\end{itemize}

\textbf{Training.} All models trained for 200 epochs with SGD (momentum 0.9), learning rate 0.1 with cosine annealing, weight decay $5 \times 10^{-4}$, and batch size 128. Each configuration runs with three seeds $\{42, 123, 456\}$.

\subsection{Results: The Gap Persists}

\begin{table}[ht]
\centering
\caption{Accuracy gap (\%) between FP32 and BitNet across augmentation strategies on CIFAR-10 with ResNet-18. The gap remains approximately 3.5\% regardless of augmentation intensity.}
\label{tab:augmentation}
\begin{tabular}{lcccc}
\toprule
Augmentation & FP32 (\%) & BitNet (\%) & Gap (\%) \\
\midrule
Basic        & 88.89 $\pm$ 0.12 & 85.40 $\pm$ 0.51 & -3.49 \\
Cutout       & 89.21 $\pm$ 0.15 & 85.74 $\pm$ 0.38 & -3.47 \\
RandAugment  & 89.45 $\pm$ 0.18 & 85.92 $\pm$ 0.42 & -3.53 \\
Full         & 89.67 $\pm$ 0.14 & 86.18 $\pm$ 0.35 & -3.49 \\
\bottomrule
\end{tabular}
\end{table}

The results are striking: \textbf{the accuracy gap remains constant at approximately 3.5\% regardless of augmentation strategy}. While stronger augmentation improves both FP32 and BitNet models in absolute terms, the \emph{relative} gap stays unchanged. This pattern holds across all model-dataset combinations we tested.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{figures/augmentation_paradox.pdf}
\caption{\textbf{The Augmentation Paradox.} Left: Both FP32 and BitNet improve with stronger augmentation. Right: The accuracy gap remains constant at $\sim$3.5\% regardless of strategy, ruling out regularization as a solution.}
\label{fig:augmentation_gap}
\end{figure}

\subsection{Implications}

This ``augmentation paradox'' rules out a class of solutions. The accuracy gap does not stem from:
\begin{itemize}
    \item Insufficient regularization
    \item Overfitting due to limited capacity
    \item Need for more diverse training data
\end{itemize}

Instead, the gap appears to reflect a \emph{fundamental representation limitation}: certain patterns that FP32 weights can represent simply cannot be approximated by ternary weights. This motivates our investigation into \emph{where} in the network this limitation manifests.

% =============================================================================
% 5. WHAT WORKS: A PRACTICAL RECIPE
% =============================================================================

\section{What Works: A Practical Recipe}
\label{sec:recipe}

Having established that augmentation cannot close the gap, we investigate targeted interventions: identifying which layers are most affected by quantization and applying knowledge distillation.

\subsection{Layer-wise Ablation: Identifying the Bottleneck}

We conduct layer-wise ablation by selectively keeping individual layers in FP32 while quantizing the rest. For ResNet architectures, we test:
\begin{itemize}
    \item \textbf{keep\_conv1}: First convolutional layer (7$\times$7 conv, 64 filters)
    \item \textbf{keep\_layer1}: First residual block
    \item \textbf{keep\_layer4}: Final residual block
    \item \textbf{keep\_fc}: Final fully-connected classifier
\end{itemize}

\begin{table}[ht]
\centering
\caption{Layer-wise ablation on ResNet-18/CIFAR-10. Keeping \texttt{conv1} in FP32 recovers 58\% of the accuracy gap despite comprising only 0.08\% of parameters.}
\label{tab:ablation}
\begin{tabular}{lccc}
\toprule
Configuration & Accuracy (\%) & Gap Recovery & Params in FP32 \\
\midrule
Full BitNet (baseline) & 85.40 & 0\% & 0\% \\
\textbf{keep\_conv1}   & \textbf{87.40} & \textbf{58\%} & \textbf{0.08\%} \\
keep\_layer1           & 86.06 & 20\% & 2.1\% \\
keep\_layer4           & 85.30 & -3\% & 45.2\% \\
keep\_fc               & 85.50 & 3\% & 0.5\% \\
\midrule
FP32 (target)          & 88.89 & 100\% & 100\% \\
\bottomrule
\end{tabular}
\end{table}

The results reveal a striking asymmetry: \textbf{\texttt{conv1} accounts for 54--74\% of the accuracy gap across all configurations}, despite comprising only 0.08\% of parameters. In contrast, keeping the entire final residual block (\texttt{layer4}, 45\% of parameters) provides no benefit---and even slightly hurts performance on CIFAR-100.

\begin{figure}[ht]
\centering
\includegraphics[width=0.9\linewidth]{figures/layer_ablation.pdf}
\caption{\textbf{Layer-wise ablation on ResNet-18/CIFAR-10.} The first convolutional layer accounts for 58\% of the accuracy gap despite comprising only 0.08\% of parameters. Later layers provide minimal benefit.}
\label{fig:layer_ablation}
\end{figure}

\textbf{Why \texttt{conv1}?} The first convolutional layer processes raw pixel intensities, where subtle gradients encode critical visual information. Ternary weights, restricted to $\{-1, 0, +1\}$, cannot capture these fine-grained patterns. Deeper layers operate on increasingly abstract, discrete-like features (edges, textures, parts) that tolerate coarse quantization.

\subsection{Knowledge Distillation: Learning Soft Labels}

Knowledge distillation (KD) transfers knowledge from a full-precision teacher to the quantized student via soft labels~\cite{hinton2015distilling}. The combined loss is:
\begin{equation}
    \mathcal{L} = \alpha \cdot \mathcal{L}_{\text{KD}} + (1 - \alpha) \cdot \mathcal{L}_{\text{CE}}
\end{equation}
where $\mathcal{L}_{\text{KD}}$ is KL divergence between softened teacher and student outputs (temperature $T=4$), $\mathcal{L}_{\text{CE}}$ is standard cross-entropy, and $\alpha=0.9$ weights the soft loss.

\begin{table}[ht]
\centering
\caption{Knowledge distillation results on CIFAR-10/100. KD recovers 36\% of the gap on CIFAR-10 and 57\% on CIFAR-100, with notably reduced variance.}
\label{tab:kd}
\begin{tabular}{lccc}
\toprule
Dataset & BitNet (\%) & BitNet + KD (\%) & Gap Recovery \\
\midrule
CIFAR-10  & 85.40 $\pm$ 0.51 & 86.66 $\pm$ 0.18 & 36\% \\
CIFAR-100 & 58.06 $\pm$ 0.45 & 60.55 $\pm$ 0.21 & 57\% \\
\bottomrule
\end{tabular}
\end{table}

KD provides two benefits: (1) accuracy improvement, especially on harder tasks (57\% recovery on CIFAR-100 vs 36\% on CIFAR-10), and (2) significantly reduced variance, indicating more stable training.

\subsection{The Complete Recipe: Combining Both}

We combine both interventions: keeping \texttt{conv1} in FP32 and training with knowledge distillation. The effects are largely additive.

\begin{table}[ht]
\centering
\caption{The complete recipe on ResNet-18/CIFAR-10. Combining \texttt{conv1} (FP32) with knowledge distillation recovers 88\% of the accuracy gap.}
\label{tab:recipe}
\begin{tabular}{lcc}
\toprule
Method & Accuracy (\%) & Gap Recovery \\
\midrule
FP32 (target) & 88.89 & --- \\
\midrule
BitNet (baseline) & 85.40 $\pm$ 0.51 & 0\% \\
BitNet + KD & 86.66 $\pm$ 0.18 & 36\% \\
BitNet + keep\_conv1 & 87.40 & 58\% \\
\textbf{BitNet + conv1 + KD} & \textbf{88.48 $\pm$ 0.17} & \textbf{88\%} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Final result (CIFAR-10)}: 88.48\% accuracy, only 0.41\% below FP32, while maintaining 19.9$\times$ compression (vs 20.3$\times$ for full BitNet). The 0.08\% parameter overhead for \texttt{conv1} is negligible.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{figures/recipe_progression.pdf}
\caption{\textbf{Recipe progression across datasets.} The combination of keeping \texttt{conv1} in FP32 and knowledge distillation recovers 88\% of the gap on CIFAR-10 and \emph{exceeds FP32} on harder tasks (CIFAR-100: +1.0\%, Tiny ImageNet: +1.3\%).}
\label{fig:recipe_progression}
\end{figure}

\textbf{CIFAR-100 Results.} On the harder 100-class task, the recipe performs even better. Table~\ref{tab:recipe_cifar100} shows that combining \texttt{conv1} with KD achieves \textbf{63.40\% accuracy---exceeding the FP32 baseline (62.40\%) by 1.0 percentage point}. This represents 123\% ``gap recovery,'' demonstrating that KD's regularization effect can push ternary networks \emph{beyond} full-precision accuracy on challenging tasks.

\begin{table}[h]
\centering
\caption{Combined recipe on CIFAR-100 (ResNet-18). The recipe \textbf{exceeds FP32 accuracy}.}
\label{tab:recipe_cifar100}
\begin{tabular}{lcc}
\toprule
Method & Accuracy (\%) & vs FP32 \\
\midrule
FP32 (baseline) & 62.40 & --- \\
\midrule
BitNet & 58.06 $\pm$ 0.45 & $-4.34$ \\
BitNet + KD & 60.55 $\pm$ 0.21 & $-1.85$ \\
BitNet + keep\_conv1 & 61.27 & $-1.13$ \\
\textbf{BitNet + conv1 + KD} & \textbf{63.40 $\pm$ 0.09} & \textbf{+1.00} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Scale Validation: Tiny ImageNet}

To validate that our recipe generalizes beyond CIFAR, we evaluate on Tiny ImageNet~\cite{le2015tinyimagenet}: 200 classes at 64$\times$64 resolution, representing a significantly harder task than CIFAR-100.

\begin{table}[ht]
\centering
\caption{Tiny ImageNet results (ResNet-18). The recipe \textbf{exceeds FP32 by 1.3 percentage points}, confirming the pattern observed on CIFAR-100.}
\label{tab:tinyimagenet}
\begin{tabular}{lcc}
\toprule
Method & Accuracy (\%) & vs FP32 \\
\midrule
FP32 (baseline) & 54.85 $\pm$ 0.16 & --- \\
\midrule
BitNet & 49.04 $\pm$ 0.19 & $-5.81$ \\
\textbf{BitNet + conv1 + KD} & \textbf{56.15} & \textbf{+1.30} \\
\bottomrule
\end{tabular}
\end{table}

The 5.81\% baseline gap is larger than CIFAR (3.5--4.3\%), confirming that the gap grows with task complexity. Critically, the recipe achieves 122\% gap recovery, exceeding FP32 by 1.3 percentage points. This confirms a key pattern: \emph{the harder the task, the more KD regularization benefits ternary networks}---pushing them beyond full-precision accuracy.

\begin{figure}[ht]
\centering
\includegraphics[width=0.9\linewidth]{figures/recipe_vs_fp32.pdf}
\caption{\textbf{Recipe exceeds FP32 on harder tasks.} While the recipe recovers 88\% of the gap on CIFAR-10 (simple task), it actually \emph{exceeds} FP32 accuracy on CIFAR-100 (+1.0\%) and Tiny ImageNet (+1.3\%). Harder tasks benefit more from KD regularization.}
\label{fig:recipe_vs_fp32}
\end{figure}

\subsection{Deployment Implications}

Our recipe provides a practical path for deploying ternary CNNs:
\begin{enumerate}
    \item Train an FP32 teacher model
    \item Convert to BitNet, keeping \texttt{conv1} in FP32
    \item Train with knowledge distillation ($T=4$, $\alpha=0.9$)
\end{enumerate}

The minimal accuracy loss (0.41\%) makes this approach viable for applications where the 20$\times$ compression and potential inference speedup outweigh the small accuracy trade-off.

% =============================================================================
% 6. DISCUSSION
% =============================================================================

\section{Discussion}
\label{sec:discussion}

\textbf{Why Does This Recipe Work?}
Our findings suggest a hierarchical view of quantization sensitivity. Early layers process continuous, fine-grained signals (pixel intensities) that require precise weight values. Later layers operate on increasingly abstract, categorical representations that tolerate coarse quantization. Knowledge distillation complements this by providing richer training signal---the soft label distribution encodes inter-class relationships that hard labels cannot capture.

\textbf{The Gap Scales with Task Complexity.}
We observe that the accuracy gap between FP32 and ternary models grows with task complexity: 3.49\% on CIFAR-10 (10 classes), 4.34\% on CIFAR-100 (100 classes), and 5.81\% on Tiny ImageNet (200 classes) (Figure~\ref{fig:gap_scaling}). This scaling is consistent with information-theoretic predictions: ternary weights carry only $\log_2(3) \approx 1.58$ bits, while the output entropy requirement $H(Y) = \log_2(C)$ grows with the number of classes~\cite{blumenfeld2019meanfield}. As the classification task demands more information capacity, the ternary network operates closer to its fundamental limit. This phenomenon---widely observed but never formally quantified in the binary/ternary literature---suggests that our recipe becomes even more critical for large-scale deployment.

\begin{figure}[ht]
\centering
\includegraphics[width=0.7\linewidth]{figures/gap_scaling.pdf}
\caption{\textbf{Gap scaling with task complexity.} The accuracy gap between FP32 and BitNet grows roughly logarithmically with the number of classes, consistent with information-theoretic predictions about ternary capacity limits.}
\label{fig:gap_scaling}
\end{figure}

\textbf{Why Lower $\alpha$ Works for Ternary Networks.}
Our hyperparameter search revealed that ternary networks prefer $\alpha=0.5$--$0.7$ (more hard labels) rather than the literature standard $\alpha=0.9$. This contradicts conventional KD wisdom but has a coherent explanation: quantization already provides strong implicit regularization~\cite{kim2019qkd}. Tang et al.~\cite{tang2020understanding} decomposed KD's benefits into label smoothing, example re-weighting, and inter-class structure. For ternary networks, the label-smoothing component becomes redundant---stacking soft-label regularization on top of quantization-induced regularization causes over-regularization that hurts the capacity-constrained student. Harder tasks (CIFAR-100) benefit even more from hard labels because the teacher's soft predictions are noisier across 100 classes, introducing more bias than useful information.

\textbf{Comparison with TTQ.}
Trained Ternary Quantization (TTQ)~\cite{zhu2017ttq} achieves near-zero accuracy gap by learning two asymmetric scaling factors per layer. This raises a natural question: why not use TTQ? The answer lies in deployment complexity. TTQ's learned scales ($W^p$, $W^n$) require floating-point storage and computation during inference. \bitnetb{}'s fixed $\{-1,0,+1\}$ values enable pure integer arithmetic---weights can be stored as 2-bit integers and convolutions become additions. Our recipe recovers most of the accuracy gap (88\%) while preserving this inference simplicity. For applications where deployment efficiency is paramount, the remaining 0.41\% accuracy loss may be an acceptable tradeoff.

\textbf{Generalization.}
Our recipe generalizes across task difficulty: on CIFAR-10 it recovers 88\% of the gap, while on CIFAR-100 it \emph{exceeds} FP32 by 1.0\%, and on Tiny ImageNet by 1.3\%. The stronger KD benefit on harder tasks suggests that soft labels provide richer training signal when the classification problem is more challenging. The consistent pattern across three datasets (10, 100, and 200 classes) indicates the underlying principles should extend to other architectures, though specific recovery percentages may vary.

\textbf{Limitations.}
Our study has several limitations:
\begin{itemize}
    \item \textbf{Training recipe}: We use a standard training recipe (SGD, cosine schedule, 200 epochs) rather than SOTA-optimized settings. This ensures fair comparison between FP32 and BitNet but leaves performance on the table. Modern binary/ternary networks achieve strong ImageNet results using Adam optimizers, 256--512 epochs, and progressive multi-stage training~\cite{liu2020reactnet,martinez2020real2binary}. Our preliminary ImageNet experiments with the CIFAR recipe showed a 26\% accuracy gap---far larger than TWN's 4\% or TTQ's 2.7\%---indicating that \bitnetb{}'s LLM-derived training recipe requires CNN-specific adaptation at scale.
    \item \textbf{KD hyperparameters at scale}: Our KD recipe ($T=4$, $\alpha=0.9$) works well up to 200 classes but may require adjustment for ImageNet's 1000 classes. QKD~\cite{kim2019qkd} showed that naive KD can hurt quantized models when the capacity gap is large; techniques like teacher assistants~\cite{mirzadeh2020teacher} or lower $\alpha$ values may be necessary.
    \item \textbf{No real latency measurements}: We report theoretical compression ($20\times$) but do not measure actual inference speedup, which depends on hardware and kernel implementations.
    \item \textbf{Classification only}: Object detection, segmentation, and other tasks may exhibit different quantization sensitivity.
\end{itemize}

% =============================================================================
% 7. CONCLUSION
% =============================================================================

\section{Conclusion}
\label{sec:conclusion}

We investigated practical methods for deploying ternary (1.58-bit) CNNs with minimal accuracy loss. Our key findings:

\begin{enumerate}
    \item \textbf{Augmentation fails}: Data augmentation cannot close the $\sim$3.5\% accuracy gap between ternary and full-precision CNNs, regardless of strategy.
    \item \textbf{\texttt{conv1} is critical}: The first convolutional layer accounts for 54--74\% of the accuracy gap despite comprising only 0.08\% of parameters.
    \item \textbf{A practical recipe exists}: Keeping \texttt{conv1} in FP32 combined with knowledge distillation recovers 88\% of the accuracy gap on CIFAR-10 (88.48\% vs 88.89\% FP32) and \emph{exceeds FP32 on harder tasks}: +1.0\% on CIFAR-100, +1.3\% on Tiny ImageNet---while maintaining $\sim$20$\times$ compression.
\end{enumerate}

Our message to practitioners: \textbf{for ternary CNNs, invest in distillation, not augmentation}. The recipe is simple to implement and provides a practical path for edge deployment with near-full-precision accuracy.

\textbf{Future Work.} Several promising directions emerge from our findings:

\begin{enumerate}
    \item \textbf{ImageNet-scale training recipes}: Modern binary networks like ReActNet~\cite{liu2020reactnet} (69.4\%) and BNext~\cite{guo2022bnext} (80.6\%) achieve strong ImageNet accuracy through Adam optimizers, 256--512 epochs, progressive quantization, and learned per-channel scaling. Adapting these techniques to \bitnetb{} while preserving its deployment simplicity (fixed $\{-1,0,+1\}$ weights) is a key direction.
    \item \textbf{Feature-level distillation}: Recent work~\cite{elthakeb2020dcq} shows that matching intermediate feature representations can provide 5--20\% additional improvement for extremely quantized networks. This may be critical for scaling KD to 1000-class tasks where logit-only distillation struggles.
    \item \textbf{Inference latency}: Measuring actual speedup with optimized ternary kernels (e.g., BitBLAS) on edge hardware.
    \item \textbf{Other tasks}: Extending to object detection, segmentation, and other vision tasks that may exhibit different quantization sensitivity patterns.
\end{enumerate}

Interestingly, our hyperparameter ablations found that individual changes---higher temperature ($T=5$--$6$) or lower $\alpha$ ($0.5$--$0.7$)---each improved results in isolation, but \emph{combining} them yielded no improvement or slight degradation. We therefore recommend the standard defaults ($T=4$, $\alpha=0.9$), which work reliably without tuning. This interaction between KD hyperparameters and quantization merits further investigation.

% =============================================================================
% ACKNOWLEDGMENTS (for camera-ready)
% =============================================================================

% \section*{Acknowledgments}
% \todo{Add acknowledgments for camera-ready version.}

% =============================================================================
% REFERENCES
% =============================================================================

\bibliographystyle{plain}
% \bibliography{references}  % Uncomment when references.bib exists

% Temporary inline references for skeleton
\begin{thebibliography}{99}

\bibitem{wang2023bitnet}
H.~Wang et al.
\newblock BitNet: Scaling 1-bit Transformers for Large Language Models.
\newblock \emph{arXiv:2310.11453}, 2023.

\bibitem{ma2024bitnetb158}
S.~Ma et al.
\newblock The Era of 1-bit LLMs: All Large Language Models are in 1.58 Bits.
\newblock \emph{arXiv:2402.17764}, 2024.

\bibitem{courbariaux2015binaryconnect}
M.~Courbariaux et al.
\newblock BinaryConnect: Training Deep Neural Networks with Binary Weights during Propagations.
\newblock \emph{NeurIPS}, 2015.

\bibitem{rastegari2016xnornet}
M.~Rastegari et al.
\newblock XNOR-Net: ImageNet Classification Using Binary Convolutional Neural Networks.
\newblock \emph{ECCV}, 2016.

\bibitem{li2016ternary}
F.~Li et al.
\newblock Ternary Weight Networks.
\newblock \emph{arXiv:1605.04711}, 2016.

\bibitem{krizhevsky2009cifar}
A.~Krizhevsky.
\newblock Learning Multiple Layers of Features from Tiny Images.
\newblock Technical report, 2009.

\bibitem{devries2017cutout}
T.~DeVries and G.~Taylor.
\newblock Improved Regularization of Convolutional Neural Networks with Cutout.
\newblock \emph{arXiv:1708.04552}, 2017.

\bibitem{cubuk2019autoaugment}
E.~Cubuk et al.
\newblock AutoAugment: Learning Augmentation Strategies from Data.
\newblock \emph{CVPR}, 2019.

\bibitem{cubuk2020randaugment}
E.~Cubuk et al.
\newblock RandAugment: Practical Automated Data Augmentation with a Reduced Search Space.
\newblock \emph{NeurIPS}, 2020.

\bibitem{rw2019timm}
R.~Wightman.
\newblock PyTorch Image Models.
\newblock \url{https://github.com/rwightman/pytorch-image-models}, 2019.

\bibitem{he2016resnet}
K.~He et al.
\newblock Deep Residual Learning for Image Recognition.
\newblock \emph{CVPR}, 2016.

\bibitem{simonyan2014vgg}
K.~Simonyan and A.~Zisserman.
\newblock Very Deep Convolutional Networks for Large-Scale Image Recognition.
\newblock \emph{ICLR}, 2015.

\bibitem{sandler2018mobilenetv2}
M.~Sandler et al.
\newblock MobileNetV2: Inverted Residuals and Linear Bottlenecks.
\newblock \emph{CVPR}, 2018.

\bibitem{tan2019efficientnet}
M.~Tan and Q.~Le.
\newblock EfficientNet: Rethinking Model Scaling for Convolutional Neural Networks.
\newblock \emph{ICML}, 2019.

\bibitem{hinton2015distilling}
G.~Hinton, O.~Vinyals, and J.~Dean.
\newblock Distilling the Knowledge in a Neural Network.
\newblock \emph{NeurIPS Deep Learning Workshop}, 2015.

\bibitem{zhu2017ttq}
C.~Zhu, S.~Han, H.~Mao, and W.~Dally.
\newblock Trained Ternary Quantization.
\newblock \emph{ICLR}, 2017.

\bibitem{kim2019qkd}
J.~Kim, Y.~Bhalgat, J.~Lee, C.~Patel, and N.~Kwak.
\newblock QKD: Quantization-aware Knowledge Distillation.
\newblock \emph{arXiv:1911.12491}, 2019.

\bibitem{wang2019haq}
K.~Wang, Z.~Liu, Y.~Lin, J.~Lin, and S.~Han.
\newblock HAQ: Hardware-Aware Automated Quantization with Mixed Precision.
\newblock \emph{CVPR}, 2019.

\bibitem{nielsen2024bitnetreloaded}
J.~Nielsen and P.~Schneider-Kamp.
\newblock BitNet b1.58 Reloaded: State-of-the-art Performance Also on Smaller Networks.
\newblock \emph{arXiv:2407.09527}, 2024.

\bibitem{kim2025bdnet}
D.~Kim, J.-S.~Lee, N.-r.~Kim, S.~Lee, and J.-H.~Lee.
\newblock BD-Net: Has Depth-Wise Convolution Ever Been Applied in Binary Neural Networks?
\newblock \emph{AAAI}, 2026.

\bibitem{zhou2016dorefa}
S.~Zhou, Y.~Wu, Z.~Ni, X.~Zhou, H.~Wen, and Y.~Zou.
\newblock DoReFa-Net: Training Low Bitwidth Convolutional Neural Networks with Low Bitwidth Gradients.
\newblock \emph{arXiv:1606.06160}, 2016.

\bibitem{dong2019hawq}
Z.~Dong, Z.~Yao, A.~Gholami, M.~Mahoney, and K.~Keutzer.
\newblock HAWQ: Hessian AWare Quantization of Neural Networks with Mixed-Precision.
\newblock \emph{ICCV}, 2019.

\bibitem{elthakeb2020dcq}
A.~T.~Elthakeb, P.~Pilligundla, F.~Mireshghallah, A.~Alaghi, and H.~Esmaeilzadeh.
\newblock Divide and Conquer: Leveraging Intermediate Feature Representations for Quantized Training of Neural Networks.
\newblock \emph{arXiv:1906.06033}, 2020.

\bibitem{blumenfeld2019meanfield}
Y.~Blumenfeld, D.~Gilboa, and D.~Soudry.
\newblock A Mean Field Theory of Quantized Deep Networks: The Quantization-Depth Trade-Off.
\newblock \emph{NeurIPS}, 2019.

\bibitem{tang2020understanding}
J.~Tang, S.~Shivanna, Z.~Zhao, D.~Lin, A.~Singh, E.~H.~Chi, and S.~Jain.
\newblock Understanding and Improving Knowledge Distillation.
\newblock \emph{arXiv:2002.03532}, 2020.

\bibitem{le2015tinyimagenet}
Y.~Le and X.~Yang.
\newblock Tiny ImageNet Visual Recognition Challenge.
\newblock CS 231N, Stanford University, 2015.

\bibitem{liu2020reactnet}
Z.~Liu, Z.~Shen, M.~Savvides, and K.-T.~Cheng.
\newblock ReActNet: Towards Precise Binary Neural Network with Generalized Activation Functions.
\newblock \emph{ECCV}, 2020.

\bibitem{martinez2020real2binary}
B.~Martinez, J.~Yang, A.~Bulat, and G.~Tzimiropoulos.
\newblock Training Binary Neural Networks with Real-to-Binary Convolutions.
\newblock \emph{ICLR}, 2020.

\bibitem{mirzadeh2020teacher}
S.~Mirzadeh, M.~Farajtabar, A.~Li, N.~Levine, A.~Matsukawa, and H.~Ghasemzadeh.
\newblock Improved Knowledge Distillation via Teacher Assistant.
\newblock \emph{AAAI}, 2020.

\bibitem{guo2022bnext}
N.~Guo, J.~Bethge, C.~Meinel, and H.~Yang.
\newblock BNext: Any Precision Binary Neural Network.
\newblock \emph{arXiv:2211.12933}, 2022.

\bibitem{cho2019efficacy}
J.~H.~Cho and B.~Hariharan.
\newblock On the Efficacy of Knowledge Distillation.
\newblock \emph{ICCV}, 2019.

\bibitem{anderson2018binary}
A.~G.~Anderson and C.~P.~Berg.
\newblock The High-Dimensional Geometry of Binary Neural Networks.
\newblock \emph{ICLR}, 2018.

\bibitem{tishby2015deep}
N.~Tishby and N.~Zaslavsky.
\newblock Deep Learning and the Information Bottleneck Principle.
\newblock \emph{IEEE Information Theory Workshop}, 2015.

\end{thebibliography}

% =============================================================================
% APPENDIX (optional)
% =============================================================================

\appendix

\section{Reproducibility: Code to Paper Pipeline}
\label{app:reproducibility}

A core contribution of this work is a fully reproducible research pipeline. Cloning our repository and running a single command regenerates all results and compiles this document.

\subsection{Repository Structure}

\begin{verbatim}
bitnet/
+-- experiments/           # Training scripts
|   +-- train.py          # Standard training
|   +-- train_kd.py       # Knowledge distillation
|   +-- sweep.py          # Batch experiment runner
+-- analysis/             # Result processing
|   +-- aggregate_results.py
|   +-- generate_tables.py
|   +-- generate_figures.py
+-- results/raw/          # Experiment outputs
|   +-- {dataset}/{model}/{config}_s{seed}/
|       +-- config.json   # Training configuration
|       +-- results.json  # Final metrics
+-- paper/
    +-- main.tex          # This document
    +-- tables/           # Generated LaTeX tables
    +-- figures/          # Generated plots
\end{verbatim}

\subsection{Regenerating the Paper}

To regenerate all figures and tables from stored results:
\begin{verbatim}
# Generate analysis artifacts
uv run python -m analysis.aggregate_results
uv run python -m analysis.generate_tables
uv run python -m analysis.generate_figures

# Compile paper
cd paper && pdflatex main.tex
\end{verbatim}

To re-run all 195+ experiments from scratch (approximately 300 GPU-hours on 2x A6000):
\begin{verbatim}
uv run python -m experiments.sweep \
    --models resnet18 resnet50 \
    --datasets cifar10 cifar100 \
    --seeds 42 123 456
\end{verbatim}

\subsection{Determinism Guarantees}

All experiments are deterministic given the same seed:
\begin{itemize}
    \item \texttt{torch.manual\_seed(seed)}
    \item \texttt{torch.cuda.manual\_seed\_all(seed)}
    \item \texttt{torch.backends.cudnn.deterministic = True}
    \item \texttt{PYTHONHASHSEED} environment variable set
\end{itemize}

Re-running with seeds $\{42, 123, 456\}$ produces identical results (verified across different machines with same CUDA version).

\subsection{Hardware and Software}

\begin{itemize}
    \item \textbf{Hardware}: 2x NVIDIA RTX A6000 (48GB VRAM each)
    \item \textbf{Software}: PyTorch 2.1+, CUDA 12.4, Python 3.11
    \item \textbf{Training time}: $\sim$45 min per CIFAR experiment, $\sim$2 hours per Tiny ImageNet experiment
    \item \textbf{Total compute}: $\sim$300 GPU-hours for all 195+ experiments
\end{itemize}

\subsection{Result Verification}

The \texttt{results/raw/} directory contains JSON files with all metrics. Anyone can:
\begin{enumerate}
    \item Clone the repository with our JSON results included
    \item Run \texttt{make paper} to regenerate figures/tables and compile
    \item Verify the PDF matches our submission
    \item Optionally re-run experiments to verify JSON reproducibility
\end{enumerate}

Code and results: \url{https://github.com/LabStrangeLoop/bitnet}

% =============================================================================
% APPENDIX B: Information-Theoretic Perspective
% =============================================================================

\section{Information-Theoretic Perspective}
\label{app:information_theory}

This appendix provides theoretical grounding for our empirical observations using information theory and the Data Processing Inequality.

\subsection{Why 1.58 Bits?}

The maximum information content of a ternary weight $w \in \{-1, 0, +1\}$ is given by the Shannon entropy:
\begin{equation}
    H(W) = -\sum_{w \in \{-1,0,+1\}} p(w) \log_2 p(w) \leq \log_2(3) \approx 1.585 \text{ bits}
\end{equation}

Equality holds when $p(-1) = p(0) = p(+1) = 1/3$. \bitnetb{}'s absmean quantization produces approximately 31\% zeros under Gaussian weight priors, yielding near-maximum entropy of $\sim$1.58 bits in practice~\cite{ma2024bitnetb158}. The theoretical compression ratio is $32 / 1.58 \approx 20.3\times$.

\subsection{Why \texttt{conv1} Matters: Information Bottleneck}

The Data Processing Inequality (DPI) states that for a Markov chain $X \to T_1 \to T_2 \to \cdots \to \hat{Y}$:
\begin{equation}
    I(X; T_1) \geq I(X; T_2) \geq \cdots \geq I(X; \hat{Y})
\end{equation}

Information about the input can only be \emph{lost}, never recovered, at each layer. The first layer sets an absolute ceiling on mutual information available to all downstream layers. Quantizing \texttt{conv1} reduces $I(X; T_1)$, and this reduction propagates as a hard upper bound.

\texttt{conv1} is uniquely vulnerable for three reasons:
\begin{enumerate}
    \item \textbf{Low input dimensionality}: With only 3 input channels, ternary weight vectors cannot reliably approximate full-precision directions~\cite{anderson2018binary}. Later layers operate on 64--512 channels where ternary approximation is geometrically more faithful.
    \item \textbf{No prior compression}: Later layers benefit from operating on representations that have already been compressed to retain task-relevant information (the Information Bottleneck principle~\cite{tishby2015deep}). \texttt{conv1} must encode \emph{all} input information before any task-relevant filtering.
    \item \textbf{Raw pixel processing}: Subtle intensity gradients in raw pixels carry critical information that ternary weights cannot capture.
\end{enumerate}

\subsection{Gap Scaling with Task Complexity}

For a $C$-class problem, the minimum information that must flow from input to output is $H(Y) = \log_2(C)$: 3.32 bits for CIFAR-10, 6.64 bits for CIFAR-100, and 7.64 bits for Tiny ImageNet (200 classes). As $C$ grows, the network must transmit more task-relevant bits through its capacity-limited ternary layers.

Our empirical gaps scale roughly with $\log_2(C)$: 3.5\% for CIFAR-10, 4.3\% for CIFAR-100, and 5.8\% for Tiny ImageNet. This logarithmic relationship suggests a fundamental capacity constraint, though the precise gap depends heavily on training recipe---as demonstrated by prior work achieving much smaller gaps on ImageNet with specialized techniques~\cite{liu2020reactnet,guo2022bnext}.

\subsection{Why KD Helps More on Harder Tasks}

A one-hot label carries $\log_2(C)$ bits. A soft label encodes inter-class similarity structure---the number of pairwise relationships scales as $\binom{C}{2} = C(C-1)/2$. For CIFAR-10, this is 45 pairs; for CIFAR-100, it is 4,950 pairs---a 110$\times$ increase in relational information.

This explains why KD recovers 36\% on CIFAR-10 but 57\% on CIFAR-100: the proportional information advantage of soft labels grows with $C$, providing the capacity-constrained ternary student with increasingly valuable guidance about how to allocate its limited representational capacity.

\end{document}
